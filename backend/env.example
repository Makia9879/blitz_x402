# 区块链RPC节点
RPC_URL=http://127.0.0.1:8545

# 链ID
CHAIN_ID=1337

# 智能合约地址（部署后填写，如果不需要合约可留空）
CONTRACT_ADDRESS=

# MON代币合约地址（如果使用ERC20版本的MON，填写地址；如果使用原生MON，可以留空）
# 注意：当前合约使用ERC20接口，如果使用原生MON，需要修改合约支持payable
MON_ADDRESS=

# 中转站钱包地址（用户充值打到这里）
TRANSIT_WALLET=0x你的中转站钱包地址

# 后端私钥（用于调用合约的adminDeposit函数，如果不需要合约可留空）
# 注意：请妥善保管私钥，不要提交到版本控制系统
PRIVATE_KEY=

# MySQL 连接
MYSQL_DSN=mysql+pymysql://blitz:blitz_pwd@localhost:3306/blitz_x402

# 可选：Thirdweb配置（用于x402集成）
THIRDWEB_SECRET_KEY=

# ============================================
# Claude API 代理配置
# ============================================

# 后端 Claude 代理服务地址（必填）
# 示例：https://your-claude-relay.com/api/v1/messages
CLAUDE_BACKEND_URL=

# Claude API Key（必填）
# 示例：sk-ant-xxxxx
CLAUDE_API_KEY=

# MON 和 Token 的兑换比例（1 MON = 多少 tokens）
# 默认：1 MON = 100,000 tokens
MON_TO_TOKEN_RATE=10000000

# 单次请求最大 tokens 限制
MAX_TOKENS_PER_REQUEST=8192

# Claude 请求超时时间（秒）
CLAUDE_REQUEST_TIMEOUT=300

